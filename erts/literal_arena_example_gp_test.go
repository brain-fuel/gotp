// Code generated by goplus from literal_arena_example_test.gp. DO NOT EDIT.
//goplus:v v0.28.0

package erts

import (
	"bytes"
	"testing"

	"goforge.dev/goplus/std/memory"
	"goforge.dev/goplus/std/result"
)

// assayxport:law gotp.erts.literal-arena-laws
func TestLiteralArenaResetInvalidatesGenerationAndReusesCapacity(t *testing.T) {
	literals := requireLiteralArena(t)
	defer literals.Close()
	var old memory.Handle
	switch __gp_m0 := any(literals.Store(7, []byte("literal"))).(type) {
	case result.Err[memory.Handle, LiteralArenaFailure]:
		failure := __gp_m0.Err
		t.Fatal(failure)
	case result.Ok[memory.Handle, LiteralArenaFailure]:
		handle := __gp_m0.Value
		old = handle
	default:
		panic("goplus: impossible enum value in match")
	}
	switch __gp_m1 := any(literals.Bytes(7)).(type) {
	case result.Err[[]byte, LiteralArenaFailure]:
		failure := __gp_m1.Err
		t.Fatal(failure)
	case result.Ok[[]byte, LiteralArenaFailure]:
		encoded := __gp_m1.Value
		if !bytes.Equal(encoded, []byte("literal")) {
			t.Fatalf("literal = %q", encoded)
		}
	default:
		panic("goplus: impossible enum value in match")
	}
	switch __gp_m2 := any(literals.Reset()).(type) {
	case result.Err[memory.Mutation, LiteralArenaFailure]:
		failure := __gp_m2.Err
		t.Fatal(failure)
	case result.Ok[memory.Mutation, LiteralArenaFailure]:
	default:
		panic("goplus: impossible enum value in match")
	}
	if literals.Len() != 0 || literals.Stats().Used != 0 {
		t.Fatalf("reset = %d/%d", literals.Len(), literals.Stats().Used)
	}
	switch __gp_m3 := any(literals.Store(8, []byte("next"))).(type) {
	case result.Err[memory.Handle, LiteralArenaFailure]:
		failure := __gp_m3.Err
		t.Fatal(failure)
	case result.Ok[memory.Handle, LiteralArenaFailure]:
	default:
		panic("goplus: impossible enum value in match")
	}
	switch __gp_m4 := any(literals.arena.Bytes(old)).(type) {
	case result.Ok[[]byte, memory.Failure]:
		t.Fatal("old literal handle survived reset")
	case result.Err[[]byte, memory.Failure]:
		failure := __gp_m4.Err
		switch any(failure).(type) {
		case memory.InvalidHandle:
		default:
			t.Fatal(failure)
		}
	default:
		panic("goplus: impossible enum value in match")
	}
}

func TestLiteralArenaRejectsDuplicateIdentity(t *testing.T) {
	literals := requireLiteralArena(t)
	defer literals.Close()
	switch __gp_m6 := any(literals.Store(1, []byte("first"))).(type) {
	case result.Err[memory.Handle, LiteralArenaFailure]:
		failure := __gp_m6.Err
		t.Fatal(failure)
	case result.Ok[memory.Handle, LiteralArenaFailure]:
	default:
		panic("goplus: impossible enum value in match")
	}
	switch __gp_m7 := any(literals.Store(1, []byte("second"))).(type) {
	case result.Err[memory.Handle, LiteralArenaFailure]:
		failure := __gp_m7.Err
		switch any(failure).(type) {
		case DuplicateLiteral:
		default:
			t.Fatal(failure)
		}
	case result.Ok[memory.Handle, LiteralArenaFailure]:
		t.Fatal("duplicate literal was accepted")
	default:
		panic("goplus: impossible enum value in match")
	}
}

func requireLiteralArena(t *testing.T) *LiteralArena {
	switch __gp_m9 := any(NewLiteralArena(4096)).(type) {
	case result.Err[*LiteralArena, LiteralArenaFailure]:
		failure := __gp_m9.Err
		t.Fatal(failure)
		return nil
	case result.Ok[*LiteralArena, LiteralArenaFailure]:
		literals := __gp_m9.Value
		return literals
	default:
		panic("goplus: impossible enum value in match")
	}
}
