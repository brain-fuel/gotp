// Code generated by goplus from virtual_cluster.gp. DO NOT EDIT.
//goplus:v v0.28.0

package erts

import (
	"sort"

	"goforge.dev/goplus/std/option"
	"goforge.dev/goplus/std/result"
	"goforge.dev/gotp/kernel"
	"goforge.dev/gotp/term"
)

type VirtualNode struct {
	Name    string
	ID      uint32
	Runtime *kernel.Kernel
}

type VirtualCluster struct {
	byName       map[string]VirtualNode
	byID         map[uint32]VirtualNode
	disconnected map[virtualRoute]bool
}

type virtualRoute struct {
	from uint32
	to   uint32
}

type VirtualClusterReport struct {
	Rounds        int
	Reductions    int
	RoutedSignals int
}

//goplus:enum VirtualClusterFailure
type VirtualClusterFailure interface{ isVirtualClusterFailure() }

//goplus:variant (VirtualClusterFailure) InvalidVirtualNode(Detail string)
type InvalidVirtualNode struct {
	Detail string
}

func (InvalidVirtualNode) isVirtualClusterFailure() {}

//goplus:variant (VirtualClusterFailure) DuplicateVirtualNode(Name string)
type DuplicateVirtualNode struct {
	Name string
}

func (DuplicateVirtualNode) isVirtualClusterFailure() {}

// VirtualClusterFailureCases selects one handler per VirtualClusterFailure variant for VirtualClusterFailureFold.
type VirtualClusterFailureCases[R any] struct {
	InvalidVirtualNode   func(Detail string) R
	DuplicateVirtualNode func(Name string) R
}

// VirtualClusterFailureFold reduces VirtualClusterFailure by one-level case analysis.
func VirtualClusterFailureFold[R any](v VirtualClusterFailure, cs VirtualClusterFailureCases[R]) R {
	switch m := any(v).(type) {
	case InvalidVirtualNode:
		return cs.InvalidVirtualNode(m.Detail)
	case DuplicateVirtualNode:
		return cs.DuplicateVirtualNode(m.Name)
	default:
		panic("goplus: impossible enum value in VirtualClusterFailureFold")
	}
}

// VirtualClusterFailureEqOverrides carries optional per-variant hooks for VirtualClusterFailureEqualWith.
// A hook returning handled=false falls through to the derived comparison.
type VirtualClusterFailureEqOverrides struct {
	InvalidVirtualNode   func(x, y InvalidVirtualNode) (eq, handled bool)
	DuplicateVirtualNode func(x, y DuplicateVirtualNode) (eq, handled bool)
}

// VirtualClusterFailureEqualWith reports structural equality of a and b under ov.
func VirtualClusterFailureEqualWith(a, b VirtualClusterFailure, ov VirtualClusterFailureEqOverrides) bool {
	if a == nil || b == nil {
		return a == nil && b == nil
	}
	switch x := any(a).(type) {
	case InvalidVirtualNode:
		y, ok := any(b).(InvalidVirtualNode)
		if !ok {
			return false
		}
		if ov.InvalidVirtualNode != nil {
			if eq, handled := ov.InvalidVirtualNode(x, y); handled {
				return eq
			}
		}
		if x.Detail != y.Detail {
			return false
		}
		return true
	case DuplicateVirtualNode:
		y, ok := any(b).(DuplicateVirtualNode)
		if !ok {
			return false
		}
		if ov.DuplicateVirtualNode != nil {
			if eq, handled := ov.DuplicateVirtualNode(x, y); handled {
				return eq
			}
		}
		if x.Name != y.Name {
			return false
		}
		return true
	}
	return false
}

// VirtualClusterFailureEqual reports structural equality of a and b.
func VirtualClusterFailureEqual(a, b VirtualClusterFailure) bool {
	return VirtualClusterFailureEqualWith(a, b, VirtualClusterFailureEqOverrides{})
}

//goplus:method (VirtualClusterFailure) Error
func VirtualClusterFailureError(failure VirtualClusterFailure) string {
	switch __gp_m0 := any(failure).(type) {
	case InvalidVirtualNode:
		detail := __gp_m0.Detail
		return "gotp/erts: invalid virtual node: " + detail
	case DuplicateVirtualNode:
		name := __gp_m0.Name
		return "gotp/erts: duplicate virtual node: " + name
	default:
		panic("goplus: impossible enum value in match")
	}
}

// assayxport:unit gotp.erts.virtual-cluster
func NewVirtualCluster() *VirtualCluster {
	return &VirtualCluster{byName: map[string]VirtualNode{}, byID: map[uint32]VirtualNode{}, disconnected: map[virtualRoute]bool{}}
}

func (cluster *VirtualCluster) AddNode(name string, id uint32, creation uint32) result.Result[VirtualNode, VirtualClusterFailure] {
	if cluster == nil || name == "" || id == 0 {
		return result.Err[VirtualNode, VirtualClusterFailure]{Err: InvalidVirtualNode{Detail: "cluster, name, and id are required"}}
	}
	if _, present := cluster.byName[name]; present {
		return result.Err[VirtualNode, VirtualClusterFailure]{Err: DuplicateVirtualNode{Name: name}}
	}
	if _, present := cluster.byID[id]; present {
		return result.Err[VirtualNode, VirtualClusterFailure]{Err: InvalidVirtualNode{Detail: "node id is already present"}}
	}
	runtime := kernel.New(kernel.KernelConfig{Node: id, Creation: creation, NodeName: name, Remote: cluster})
	node := VirtualNode{Name: name, ID: id, Runtime: runtime}
	cluster.byName[name] = node
	cluster.byID[id] = node
	return result.Ok[VirtualNode, VirtualClusterFailure]{Value: node}
}

func (cluster *VirtualCluster) Disconnect(left uint32, right uint32) {
	cluster.disconnected[virtualRoute{from: left, to: right}] = true
	cluster.disconnected[virtualRoute{from: right, to: left}] = true
}

func (cluster *VirtualCluster) Connect(left uint32, right uint32) {
	delete(cluster.disconnected, virtualRoute{from: left, to: right})
	delete(cluster.disconnected, virtualRoute{from: right, to: left})
}

func (cluster *VirtualCluster) NodeName(node uint32) option.Option[string] {
	value, present := cluster.byID[node]
	if !present {
		return option.None[string]{}
	}
	return option.Some[string]{Value: value.Name}
}

func (cluster *VirtualCluster) ConnectedNodes(node uint32) []string {
	names := []string{}
	for id, candidate := range cluster.byID {
		if id != node && cluster.connected(node, id) {
			names = append(names, candidate.Name)
		}
	}
	sort.Strings(names)
	return names
}

func (cluster *VirtualCluster) SendPID(from term.PID, to term.PID, message term.Term) kernel.Delivery {
	target, present := cluster.byID[to.Node]
	if !present || !cluster.connected(from.Node, to.Node) {
		return kernel.NoProcess{}
	}
	return target.Runtime.Send(from, to, message)
}

func (cluster *VirtualCluster) SendName(from term.PID, node string, name string, message term.Term) kernel.Delivery {
	target, present := cluster.byName[node]
	if !present || !cluster.connected(from.Node, target.ID) {
		return kernel.NoProcess{}
	}
	return target.Runtime.SendRegistered(from, name, message)
}

func (cluster *VirtualCluster) SendAlias(from term.PID, reference term.Reference, message term.Term) kernel.Delivery {
	target, present := cluster.byID[reference.Node]
	if !present || !cluster.connected(from.Node, reference.Node) {
		return kernel.NoProcess{}
	}
	return target.Runtime.SendAlias(from, reference, message)
}

func (cluster *VirtualCluster) MonitorName(watcher term.PID, node string, name string, reference term.Reference) bool {
	target, present := cluster.byName[node]
	if !present || !cluster.connected(watcher.Node, target.ID) {
		return false
	}
	switch any(target.Runtime.MonitorRemoteName(watcher, name, reference)).(type) {
	case result.Err[kernel.KernelMutation, kernel.Failure]:
		return false
	case result.Ok[kernel.KernelMutation, kernel.Failure]:
		return true
	default:
		panic("goplus: impossible enum value in match")
	}
}

func (cluster *VirtualCluster) DemonitorName(watcher term.PID, node string, name string, reference term.Reference) {
	target, present := cluster.byName[node]
	if !present {
		return
	}
	target.Runtime.DemonitorRemoteName(watcher, name, reference)
}

func (cluster *VirtualCluster) Run(maxRounds int, reductionsPerNode int) VirtualClusterReport {
	if maxRounds <= 0 {
		maxRounds = 1
	}
	if reductionsPerNode <= 0 {
		reductionsPerNode = 1_000_000
	}
	report := VirtualClusterReport{}
	ids := make([]int, 0, len(cluster.byID))
	for id := range cluster.byID {
		ids = append(ids, int(id))
	}
	sort.Ints(ids)
	for round := 0; round < maxRounds; round++ {
		active := false
		report.Rounds++
		for _, rawID := range ids {
			node := cluster.byID[uint32(rawID)]
			run := node.Runtime.Run(reductionsPerNode)
			report.Reductions += run.Reductions
			if run.Reductions > 0 || run.Runnable > 0 {
				active = true
			}
			for _, signal := range node.Runtime.DrainRemoteSignals() {
				active = true
				report.RoutedSignals++
				switch __gp_m2 := any(signal).(type) {
				case kernel.RemoteExitSignal:
					from := __gp_m2.From
					to := __gp_m2.To
					reason := __gp_m2.Reason
					if destination, present := cluster.byID[to.Node]; present && cluster.connected(from.Node, to.Node) {
						destination.Runtime.SendExit(from, to, reason)
					}
				case kernel.RemoteDownSignal:
					source := __gp_m2.Source
					to := __gp_m2.To
					reference := __gp_m2.Reference
					reason := __gp_m2.Reason
					if destination, present := cluster.byID[to.Node]; present && cluster.connected(source.Node, to.Node) {
						destination.Runtime.DeliverRemoteDown(source, to, reference, term.PIDValue(source), reason)
					}
				case kernel.RemoteDownNamedSignal:
					source := __gp_m2.Source
					to := __gp_m2.To
					reference := __gp_m2.Reference
					reason := __gp_m2.Reason
					if destination, present := cluster.byID[to.Node]; present && cluster.connected(uint32(rawID), to.Node) {
						destination.Runtime.DeliverRemoteDown(term.PID{Node: uint32(rawID), Creation: 1}, to, reference, term.Tuple(term.MustAtom(source), term.MustAtom(node.Name)), reason)
					}
				default:
					panic("goplus: impossible enum value in match")
				}
			}
		}
		if !active {
			break
		}
	}
	return report
}

func (cluster *VirtualCluster) connected(from uint32, to uint32) bool {
	if from == to {
		return true
	}
	_, left := cluster.byID[from]
	_, right := cluster.byID[to]
	return left && right && !cluster.disconnected[virtualRoute{from: from, to: to}]
}
