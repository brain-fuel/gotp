// Code generated by goplus from upstream_java.gp. DO NOT EDIT.
//goplus:v v0.28.0

package compat

import (
	"crypto/sha256"
	"encoding/hex"
	"encoding/json"
	"fmt"
	"sort"
	"strings"

	"goforge.dev/goplus/std/option"
	"goforge.dev/goplus/std/result"
)

const (
	JavaInventorySchema    = "gotp.otp-java-api/v1"
	PinnedJavaCompiler     = "javac 21.0.12"
	PinnedJavaSourceCount  = 57
	PinnedJavaClassCount   = 65
	PinnedJavaSymbolCount  = 730
	PinnedJavaSourceDigest = "3c059636cd68ae676f60b8604b2ec72008a106dcd41ea7f4d8bf49b48fec8898"
	PinnedJavaClassDigest  = "2cee8091c1d10749787884aa96e0f70cafe898fbaf42642942ac11b8d87f74c1"
)

type OTPJavaSource struct {
	SourcePath string
	Content    []byte
}

type OTPJavaClass struct {
	ClassPath string
	Content   []byte
}

type OTPJavaAPIInventory struct {
	Schema         string       `json:"schema"`
	UpstreamTag    string       `json:"upstream_tag"`
	UpstreamCommit string       `json:"upstream_commit"`
	Compiler       string       `json:"compiler"`
	SourceDigest   string       `json:"source_digest"`
	ClassDigest    string       `json:"class_digest"`
	SourceCount    int          `json:"source_count"`
	ClassCount     int          `json:"class_count"`
	Symbols        []JavaSymbol `json:"symbols"`
}

// assayxport:unit gotp.compat.otp-java-api
func BuildOTPJavaAPIInventory(compiler string, sources []OTPJavaSource, classes []OTPJavaClass) result.Result[OTPJavaAPIInventory, JavaFailure] {
	orderedSources := append([]OTPJavaSource{}, sources...)
	sort.Slice(orderedSources, func(left, right int) bool { return orderedSources[left].SourcePath < orderedSources[right].SourcePath })
	switch __gp_m0 := any(digestJavaSources(orderedSources)).(type) {
	case result.Err[string, JavaFailure]:
		failure := __gp_m0.Err
		return result.Err[OTPJavaAPIInventory, JavaFailure]{Err: failure}
	case result.Ok[string, JavaFailure]:
		sourceDigest := __gp_m0.Value

		orderedClasses := append([]OTPJavaClass{}, classes...)
		sort.Slice(orderedClasses, func(left, right int) bool { return orderedClasses[left].ClassPath < orderedClasses[right].ClassPath })
		switch __gp_m1 := any(digestJavaClasses(orderedClasses)).(type) {
		case result.Err[string, JavaFailure]:
			failure := __gp_m1.Err
			return result.Err[OTPJavaAPIInventory, JavaFailure]{Err: failure}
		case result.Ok[string, JavaFailure]:
			classDigest := __gp_m1.Value

			symbols := map[string]JavaSymbol{}
			for _, class := range orderedClasses {
				switch __gp_m2 := any(ParseJVMClass(class.ClassPath, class.Content)).(type) {
				case result.Err[JavaClassAPI, JavaFailure]:
					failure := __gp_m2.Err
					return result.Err[OTPJavaAPIInventory, JavaFailure]{Err: failure}
				case result.Ok[JavaClassAPI, JavaFailure]:
					api := __gp_m2.Value

					for _, symbol := range api.Symbols {
						id := InventoryJavaSymbolID(symbol)
						if prior, present := symbols[id]; present && !sameJavaSymbol(prior, symbol) {
							return result.Err[OTPJavaAPIInventory, JavaFailure]{Err: JavaInventoryRejected{Detail: "symbol collision " + id}}
						}
						symbols[id] = symbol
					}
				default:
					panic("goplus: impossible enum value in match")
				}
			}
			inventory := OTPJavaAPIInventory{Schema: JavaInventorySchema, UpstreamTag: OTPPinnedTag, UpstreamCommit: OTPPinnedCommit, Compiler: compiler, SourceDigest: sourceDigest, ClassDigest: classDigest, SourceCount: len(orderedSources), ClassCount: len(orderedClasses)}
			for _, symbol := range symbols {
				inventory.Symbols = append(inventory.Symbols, symbol)
			}
			sort.Slice(inventory.Symbols, func(left, right int) bool {
				return InventoryJavaSymbolID(inventory.Symbols[left]) < InventoryJavaSymbolID(inventory.Symbols[right])
			})
			switch __gp_m3 := any(ValidateOTPJavaAPIInventory(inventory)).(type) {
			case result.Err[bool, JavaFailure]:
				failure := __gp_m3.Err
				return result.Err[OTPJavaAPIInventory, JavaFailure]{Err: failure}
			case result.Ok[bool, JavaFailure]:
				return result.Ok[OTPJavaAPIInventory, JavaFailure]{Value: inventory}
			default:
				panic("goplus: impossible enum value in match")
			}
		default:
			panic("goplus: impossible enum value in match")
		}
	default:
		panic("goplus: impossible enum value in match")
	}
}

func ParseOTPJavaAPIInventory(data []byte) result.Result[OTPJavaAPIInventory, JavaFailure] {
	var inventory OTPJavaAPIInventory
	switch __gp_m4 := any(result.Of(true, json.Unmarshal(data, &inventory))).(type) {
	case result.Err[bool, error]:
		cause := __gp_m4.Err
		return result.Err[OTPJavaAPIInventory, JavaFailure]{Err: JavaJSONRejected{Cause: cause.Error()}}
	case result.Ok[bool, error]:

	default:
		panic("goplus: impossible enum value in match")
	}
	switch __gp_m5 := any(ValidateOTPJavaAPIInventory(inventory)).(type) {
	case result.Err[bool, JavaFailure]:
		failure := __gp_m5.Err
		return result.Err[OTPJavaAPIInventory, JavaFailure]{Err: failure}
	case result.Ok[bool, JavaFailure]:

		inventory.Symbols = cloneJavaSymbols(inventory.Symbols)
		return result.Ok[OTPJavaAPIInventory, JavaFailure]{Value: inventory}
	default:
		panic("goplus: impossible enum value in match")
	}
}

func ValidateOTPJavaAPIInventory(inventory OTPJavaAPIInventory) result.Result[bool, JavaFailure] {
	if inventory.Schema != JavaInventorySchema || inventory.UpstreamTag != OTPPinnedTag || inventory.UpstreamCommit != OTPPinnedCommit {
		return result.Err[bool, JavaFailure]{Err: JavaInventoryRejected{Detail: "schema or upstream pin differs"}}
	}
	if inventory.Compiler != PinnedJavaCompiler {
		return result.Err[bool, JavaFailure]{Err: JavaInventoryRejected{Detail: "compiler differs: " + inventory.Compiler}}
	}
	if inventory.SourceDigest != PinnedJavaSourceDigest {
		return result.Err[bool, JavaFailure]{Err: JavaInventoryRejected{Detail: "source digest differs: " + inventory.SourceDigest}}
	}
	if inventory.ClassDigest != PinnedJavaClassDigest {
		return result.Err[bool, JavaFailure]{Err: JavaInventoryRejected{Detail: "class digest differs: " + inventory.ClassDigest}}
	}
	if inventory.SourceCount != PinnedJavaSourceCount || inventory.ClassCount != PinnedJavaClassCount {
		return result.Err[bool, JavaFailure]{Err: JavaInventoryRejected{Detail: fmt.Sprintf("source/class counts %d/%d", inventory.SourceCount, inventory.ClassCount)}}
	}
	if len(inventory.Symbols) != PinnedJavaSymbolCount {
		return result.Err[bool, JavaFailure]{Err: JavaInventoryRejected{Detail: fmt.Sprintf("symbol count %d; want %d", len(inventory.Symbols), PinnedJavaSymbolCount)}}
	}
	prior := ""
	for index, symbol := range inventory.Symbols {
		if symbol.Class == "" || symbol.Name == "" || symbol.Descriptor == "" {
			return result.Err[bool, JavaFailure]{Err: JavaInventoryRejected{Detail: "empty symbol identity"}}
		}
		switch any(javaSymbolKind(symbol.Kind)).(type) {
		case option.None[JavaSymbolKind]:
			return result.Err[bool, JavaFailure]{Err: JavaInventoryRejected{Detail: "unknown symbol kind " + symbol.Kind}}
		case option.Some[JavaSymbolKind]:

		default:
			panic("goplus: impossible enum value in match")
		}
		if symbol.Visibility != "public" && symbol.Visibility != "protected" {
			return result.Err[bool, JavaFailure]{Err: JavaInventoryRejected{Detail: "non-public API visibility"}}
		}
		id := InventoryJavaSymbolID(symbol)
		if index > 0 && id <= prior {
			return result.Err[bool, JavaFailure]{Err: JavaInventoryRejected{Detail: "symbols are not strictly sorted"}}
		}
		prior = id
	}
	return result.Ok[bool, JavaFailure]{Value: true}
}

func InventoryJavaSymbolID(symbol JavaSymbol) string {
	identity := symbol.Class + "\x00" + symbol.Name + "\x00" + symbol.Descriptor + "\x00" + symbol.GenericSignature
	return "otp.java." + symbol.Kind + "." + hex.EncodeToString([]byte(identity))
}

func MissingJavaCoverage(inventory OTPJavaAPIInventory, ledger Ledger) []string {
	covered := make(map[string]struct{}, len(ledger.Items))
	for _, item := range ledger.Items {
		covered[item.ID] = struct{}{}
	}
	missing := []string{}
	for _, symbol := range inventory.Symbols {
		id := InventoryJavaSymbolID(symbol)
		if _, present := covered[id]; !present {
			missing = append(missing, id)
		}
	}
	return missing
}

func digestJavaSources(sources []OTPJavaSource) result.Result[string, JavaFailure] {
	var canonical strings.Builder
	prior := ""
	for index, source := range sources {
		if source.SourcePath == "" || (index > 0 && source.SourcePath <= prior) {
			return result.Err[string, JavaFailure]{Err: JavaInventoryRejected{Detail: "empty or duplicate Java source path"}}
		}
		canonical.WriteString(source.SourcePath)
		canonical.WriteByte(0)
		canonical.Write(source.Content)
		canonical.WriteByte(0)
		prior = source.SourcePath
	}
	digest := sha256.Sum256([]byte(canonical.String()))
	return result.Ok[string, JavaFailure]{Value: hex.EncodeToString(digest[:])}
}

func digestJavaClasses(classes []OTPJavaClass) result.Result[string, JavaFailure] {
	var canonical strings.Builder
	prior := ""
	for index, class := range classes {
		if class.ClassPath == "" || (index > 0 && class.ClassPath <= prior) {
			return result.Err[string, JavaFailure]{Err: JavaInventoryRejected{Detail: "empty or duplicate Java class path"}}
		}
		canonical.WriteString(class.ClassPath)
		canonical.WriteByte(0)
		canonical.Write(class.Content)
		canonical.WriteByte(0)
		prior = class.ClassPath
	}
	digest := sha256.Sum256([]byte(canonical.String()))
	return result.Ok[string, JavaFailure]{Value: hex.EncodeToString(digest[:])}
}

func sameJavaSymbol(left JavaSymbol, right JavaSymbol) bool {
	if left.Kind != right.Kind || left.Class != right.Class || left.Name != right.Name || left.Descriptor != right.Descriptor || left.GenericSignature != right.GenericSignature || left.MetadataDigest != right.MetadataDigest || left.Visibility != right.Visibility || left.Static != right.Static || left.Final != right.Final || left.Synthetic != right.Synthetic || left.Bridge != right.Bridge {
		return false
	}
	if len(left.Exceptions) != len(right.Exceptions) {
		return false
	}
	for index := range left.Exceptions {
		if left.Exceptions[index] != right.Exceptions[index] {
			return false
		}
	}
	return true
}

func cloneJavaSymbols(symbols []JavaSymbol) []JavaSymbol {
	cloned := append([]JavaSymbol{}, symbols...)
	for index := range cloned {
		cloned[index].Exceptions = append([]string{}, cloned[index].Exceptions...)
	}
	return cloned
}
