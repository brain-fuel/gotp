// Code generated by goplus from interpreter_test.gp. DO NOT EDIT.
//goplus:v v0.28.0

package vm

import (
	"math/big"
	"strings"
	"testing"

	"goforge.dev/goplus/std/option"
	"goforge.dev/goplus/std/result"
	"goforge.dev/gotp/beam"
	"goforge.dev/gotp/term"
)

func TestMachineRunsLocalCallAndRegisterMoves(t *testing.T) {
	program := []beam.Instruction{
		instruction("label", label(1)),
		instruction("move", integer(42), xregister(0)),
		instruction("call", unsigned(0), label(2)),
		instruction("return"),
		instruction("label", label(2)),
		instruction("move", xregister(0), xregister(1)),
		instruction("return"),
		instruction("int_code_end"),
	}
	switch __gp_m0 := any(NewMachine(program, MachineConfig{})).(type) {
	case result.Err[*Machine, Failure]:
		failure := __gp_m0.Err

		t.Fatal(Error(failure))
	case result.Ok[*Machine, Failure]:
		machine := __gp_m0.Value

		switch __gp_m1 := any(machine.Run(1)).(type) {
		case result.Err[RunResult, Failure]:
			failure := __gp_m1.Err

			t.Fatal(Error(failure))
		case result.Ok[RunResult, Failure]:
			run := __gp_m1.Value

			assertIntegerOperand(t, run.Value, 42)
		default:
			panic("goplus: impossible enum value in match")
		}
		switch __gp_m2 := any(machine.X(1)).(type) {
		case result.Err[term.Term, Failure]:
			failure := __gp_m2.Err

			t.Fatal(Error(failure))
		case result.Ok[term.Term, Failure]:
			value := __gp_m2.Value

			assertIntegerOperand(t, value, 42)
		default:
			panic("goplus: impossible enum value in match")
		}
	default:
		panic("goplus: impossible enum value in match")
	}
}

func TestMachineSupportsTailCall(t *testing.T) {
	program := []beam.Instruction{
		instruction("label", label(1)),
		instruction("move", integer(9), xregister(0)),
		instruction("call_only", unsigned(0), label(2)),
		instruction("label", label(2)),
		instruction("return"),
	}
	switch __gp_m3 := any(NewMachine(program, MachineConfig{})).(type) {
	case result.Err[*Machine, Failure]:
		failure := __gp_m3.Err

		t.Fatal(Error(failure))
	case result.Ok[*Machine, Failure]:
		machine := __gp_m3.Value

		switch __gp_m4 := any(machine.Run(1)).(type) {
		case result.Err[RunResult, Failure]:
			failure := __gp_m4.Err

			t.Fatal(Error(failure))
		case result.Ok[RunResult, Failure]:
			run := __gp_m4.Value

			assertIntegerOperand(t, run.Value, 9)
		default:
			panic("goplus: impossible enum value in match")
		}
	default:
		panic("goplus: impossible enum value in match")
	}
}

func TestMachineBoundsInfiniteControlFlow(t *testing.T) {
	program := []beam.Instruction{
		instruction("label", label(1)),
		instruction("jump", label(1)),
	}
	switch __gp_m5 := any(NewMachine(program, MachineConfig{StepLimit: 10})).(type) {
	case result.Err[*Machine, Failure]:
		failure := __gp_m5.Err

		t.Fatal(Error(failure))
	case result.Ok[*Machine, Failure]:
		machine := __gp_m5.Value

		switch __gp_m6 := any(machine.Run(1)).(type) {
		case result.Err[RunResult, Failure]:
			failure := __gp_m6.Err

			if !strings.Contains(Error(failure), "step limit") {
				t.Fatalf("failure = %s", Error(failure))
			}
		case result.Ok[RunResult, Failure]:

			t.Fatal("infinite control flow completed")
		default:
			panic("goplus: impossible enum value in match")
		}
	default:
		panic("goplus: impossible enum value in match")
	}
}

func TestMachineRejectsUnsupportedOpcode(t *testing.T) {
	program := []beam.Instruction{
		instruction("label", label(1)),
		instruction("definitely_unsupported"),
	}
	switch __gp_m7 := any(NewMachine(program, MachineConfig{})).(type) {
	case result.Err[*Machine, Failure]:
		failure := __gp_m7.Err

		t.Fatal(Error(failure))
	case result.Ok[*Machine, Failure]:
		machine := __gp_m7.Value

		switch __gp_m8 := any(machine.Run(1)).(type) {
		case result.Err[RunResult, Failure]:
			failure := __gp_m8.Err

			if !strings.Contains(Error(failure), "not implemented") {
				t.Fatalf("failure = %s", Error(failure))
			}
		case result.Ok[RunResult, Failure]:

			t.Fatal("unsupported opcode completed")
		default:
			panic("goplus: impossible enum value in match")
		}
	default:
		panic("goplus: impossible enum value in match")
	}
}

func assertIntegerOperand(t *testing.T, value term.Term, want int64) {
	t.Helper()
	switch __gp_m9 := any(term.Int64(value)).(type) {
	case option.Some[int64]:
		found := __gp_m9.Value

		if found != want {
			t.Fatalf("integer = %d, want %d", found, want)
		}
	case option.None[int64]:

		t.Fatal("runtime value is not an integer")
	default:
		panic("goplus: impossible enum value in match")
	}
}

func TestMachineRegistersStoreRuntimeTerms(t *testing.T) {
	switch __gp_m10 := any(NewMachine([]beam.Instruction{}, MachineConfig{})).(type) {
	case result.Err[*Machine, Failure]:
		failure := __gp_m10.Err

		t.Fatal(Error(failure))
	case result.Ok[*Machine, Failure]:
		machine := __gp_m10.Value

		pid := term.PID{Node: 1, Number: 7, Creation: 1}
		switch __gp_m11 := any(machine.SetX(0, term.PIDValue(pid))).(type) {
		case result.Err[MachineMutation, Failure]:
			failure := __gp_m11.Err

			t.Fatal(Error(failure))
		case result.Ok[MachineMutation, Failure]:

			switch __gp_m12 := any(machine.X(0)).(type) {
			case result.Err[term.Term, Failure]:
				failure := __gp_m12.Err

				t.Fatal(Error(failure))
			case result.Ok[term.Term, Failure]:
				value := __gp_m12.Value

				switch __gp_m13 := any(term.TermPIDValue(value)).(type) {
				case option.Some[term.PID]:
					found := __gp_m13.Value

					if found != pid {
						t.Fatalf("PID = %#v", found)
					}
				case option.None[term.PID]:

					t.Fatal("register value is not a PID")
				default:
					panic("goplus: impossible enum value in match")
				}
			default:
				panic("goplus: impossible enum value in match")
			}
		default:
			panic("goplus: impossible enum value in match")
		}
	default:
		panic("goplus: impossible enum value in match")
	}
}

func instruction(name string, operands ...beam.Operand) beam.Instruction {
	return beam.Instruction{
		Opcode: beam.Opcode{Name: name, Arity: len(operands)}, Operands: operands,
	}
}

func unsigned(value int64) beam.Operand {
	return beam.UnsignedOperand{Value: big.NewInt(value)}
}

func integer(value int64) beam.Operand {
	return beam.IntegerOperand{Value: big.NewInt(value)}
}

func label(value int64) beam.Operand {
	return beam.LabelOperand{Index: big.NewInt(value)}
}

func xregister(value int64) beam.Operand {
	return beam.XRegisterOperand{Index: big.NewInt(value)}
}
