// Code generated by goplus from system_example_test.gp. DO NOT EDIT.
//goplus:v v0.28.0

package genserver

import (
	"goforge.dev/goplus/std/option"
	"goforge.dev/goplus/std/result"
	"goforge.dev/gotp/kernel"
	"goforge.dev/gotp/term"
	"testing"
)

func systemMessage(from term.PID, tag string, request term.Term) term.Term {
	return term.Tuple(systemTag, term.Tuple(term.PIDTerm{Value: from}, term.MustAtom(tag)), request)
}

// assayxport:law gotp.otp.gen-server-system-laws
func TestSystemSuspendChangeCodeResumeIsSelective(t *testing.T) {
	server := upgradeServer(t, func(_ term.Term, state int64, extra term.Term) result.Result[int64, Failure] {
		switch __gp_m0 := any(term.Int64(extra)).(type) {
		case option.None[int64]:
			return result.Err[int64, Failure]{Err: CallbackFailure{Detail: "extra is not int64"}}
		case option.Some[int64]:
			value := __gp_m0.Value
			return result.Ok[int64, Failure]{Value: state + value}
		default:
			panic("goplus: impossible enum value in match")
		}
	})
	runtime := kernel.New(kernel.KernelConfig{})
	serverPID := spawn(t, runtime, server.Behavior())
	replies := []term.Term{}
	clientPID := spawn(t, runtime, func(context *kernel.Context) kernel.StepResult {
		switch __gp_m1 := any(context.ReceiveMessage(nil)).(type) {
		case option.None[kernel.MessageEnvelope]:
			return kernel.Wait{}
		case option.Some[kernel.MessageEnvelope]:
			envelope := __gp_m1.Value
			replies = append(replies, envelope.Message)
			return kernel.Yield{}
		default:
			panic("goplus: impossible enum value in match")
		}
	})
	runtime.Send(clientPID, serverPID, systemMessage(clientPID, "suspended", term.MustAtom("suspend")))
	runtime.Run(20)
	if !server.Suspended() || server.State() != 7 {
		t.Fatalf("suspended/state = %v/%d", server.Suspended(), server.State())
	}
	runtime.Send(clientPID, serverPID, term.Tuple(castTag, term.Integer(5)))
	runtime.Send(clientPID, serverPID, systemMessage(clientPID, "changed", term.Tuple(term.MustAtom("change_code"), term.MustAtom("sample"), term.MustAtom("v1"), term.Integer(100))))
	runtime.Run(20)
	if server.State() != 107 {
		t.Fatalf("state while suspended = %d", server.State())
	}
	runtime.Send(clientPID, serverPID, systemMessage(clientPID, "resumed", term.MustAtom("resume")))
	runtime.Run(30)
	if server.Suspended() || server.State() != 112 {
		t.Fatalf("resumed/state = %v/%d", server.Suspended(), server.State())
	}
	if len(replies) != 3 {
		t.Fatalf("system replies = %d, want 3", len(replies))
	}
	for index, tag := range []string{"suspended", "changed", "resumed"} {
		switch __gp_m2 := any(replies[index]).(type) {
		case term.TupleTerm:
			values := __gp_m2.Elements
			if len(values) != 2 || !term.Equal(values[0], term.MustAtom(tag)) || !term.Equal(values[1], okReply) {
				t.Fatalf("reply %d = %v", index, replies[index])
			}
		default:
			t.Fatalf("reply %d is not tuple", index)
		}
	}
}

func TestSystemChangeCodeRequiresSuspension(t *testing.T) {
	server := upgradeServer(t, func(_ term.Term, state int64, _ term.Term) result.Result[int64, Failure] {
		return result.Ok[int64, Failure]{Value: state + 1}
	})
	runtime := kernel.New(kernel.KernelConfig{})
	serverPID := spawn(t, runtime, server.Behavior())
	clientPID := spawn(t, runtime, func(context *kernel.Context) kernel.StepResult { return kernel.Wait{} })
	runtime.Send(clientPID, serverPID, systemMessage(clientPID, "change", term.Tuple(term.MustAtom("change_code"), term.MustAtom("sample"), term.MustAtom("v1"), term.Integer(0))))
	runtime.Run(20)
	if server.State() != 7 {
		t.Fatalf("unsuspended change state = %d", server.State())
	}
}
