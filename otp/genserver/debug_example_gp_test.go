// Code generated by goplus from debug_example_test.gp. DO NOT EDIT.
//goplus:v v0.28.0

package genserver

import (
	"testing"
	"testing/quick"
	"time"

	"goforge.dev/goplus/std/clock"
	"goforge.dev/goplus/std/result"
	"goforge.dev/gotp/kernel"
	"goforge.dev/gotp/term"
)

func debugServer(t *testing.T, source clock.Clock, output DebugOutputCapability) *Server[int64, int64, int64, int64] {
	t.Helper()
	switch __gp_m0 := any(New(Config[int64, int64, int64, int64]{
		InitialState: 3, RequestCodec: Int64Codec{}, ReplyCodec: Int64Codec{}, CastCodec: Int64Codec{}, StateCodec: Int64Codec{}, Clock: source, DebugOutput: output,
		HandleCall: func(_ *kernel.Context, _ int64, state int64) result.Result[CallResult[int64, int64], Failure] {
			return result.Ok[CallResult[int64, int64], Failure]{Value: ContinueCall[int64, int64]{ReplyValue: state, StateValue: state}}
		},
		HandleCast: func(_ *kernel.Context, value int64, state int64) result.Result[EventResult[int64], Failure] {
			return result.Ok[EventResult[int64], Failure]{Value: ContinueEvent[int64]{StateValue: state + value}}
		},
	})).(type) {
	case result.Err[*Server[int64, int64, int64, int64], Failure]:
		failure := __gp_m0.Err
		t.Fatal(Error(failure))
		return nil
	case result.Ok[*Server[int64, int64, int64, int64], Failure]:
		server := __gp_m0.Value
		return server
	default:
		panic("goplus: impossible enum value in match")
	}
}

// assayxport:law gotp.otp.gen-server-debug-laws
func TestSystemDebugTraceLogAndStatistics(t *testing.T) {
	source := clock.NewFake(time.Date(2026, 7, 31, 12, 0, 0, 0, time.UTC))
	traced := []term.Term{}
	server := debugServer(t, source, DebugOutputWith{Effect: func(event term.Term, _ term.Term) { traced = append(traced, event) }})
	if !term.Equal(server.debugCommand(term.Tuple(term.MustAtom("log"), term.Tuple(term.MustAtom("true"), term.Integer(2)))), okReply) {
		t.Fatal("log enable failed")
	}
	if !term.Equal(server.debugCommand(term.Tuple(term.MustAtom("statistics"), term.MustAtom("true"))), okReply) {
		t.Fatal("statistics enable failed")
	}
	if !term.Equal(server.debugCommand(term.Tuple(term.MustAtom("trace"), term.MustAtom("true"))), okReply) {
		t.Fatal("trace enable failed")
	}
	server.debugEvent(term.Tuple(term.MustAtom("in"), term.MustAtom("first")))
	server.debugEvent(term.Tuple(term.MustAtom("out"), term.MustAtom("second"), term.PIDTerm{Value: term.PID{Node: 1, Number: 1, Creation: 1}}))
	server.debugEvent(term.Tuple(term.MustAtom("in"), term.MustAtom("third")))
	if len(traced) != 3 || len(server.debug.events) != 2 {
		t.Fatalf("trace/log counts = %d/%d", len(traced), len(server.debug.events))
	}
	switch __gp_m1 := any(server.debugLogCommand(term.MustAtom("get"))).(type) {
	case term.TupleTerm:
		values := __gp_m1.Elements
		switch __gp_m2 := any(values[1]).(type) {
		case term.ProperListTerm:
			events := __gp_m2.Elements
			if len(events) != 2 {
				t.Fatalf("logged events = %d", len(events))
			}
		default:
			t.Fatal("log response is not list")
		}
	default:
		t.Fatal("log response is not tuple")
	}
	source.Advance(2 * time.Second)
	switch __gp_m3 := any(server.debugStatisticsCommand(term.MustAtom("get"))).(type) {
	case term.TupleTerm:
		values := __gp_m3.Elements
		switch __gp_m4 := any(values[1]).(type) {
		case term.ProperListTerm:
			stats := __gp_m4.Elements
			if len(stats) != 5 {
				t.Fatalf("statistics = %d", len(stats))
			}
		default:
			t.Fatal("statistics response is not list")
		}
	default:
		t.Fatal("statistics response is not tuple")
	}
	if !term.Equal(server.debugCommand(term.MustAtom("no_debug")), okReply) || server.debug.trace || server.debug.logLimit != 0 || server.debug.statistics {
		t.Fatal("no_debug did not reset debug state")
	}
}

func TestSystemDebugRequestDecodesPinnedShape(t *testing.T) {
	request := term.Tuple(term.MustAtom("debug"), term.Tuple(term.MustAtom("log"), term.MustAtom("get")))
	switch __gp_m5 := any(systemRequestFromTerm(request)).(type) {
	case systemDebug:
		command := __gp_m5.command
		if !term.Equal(command, requestTupleValue(t, request, 1)) {
			t.Fatal("debug command differs")
		}
	default:
		t.Fatal("debug request was not decoded")
	}
}

func TestSystemDebugLogBoundLaw(t *testing.T) {
	law := func(values []int64, rawLimit uint8) bool {
		limit := int(rawLimit%32) + 1
		server := debugServer(t, clock.NewFake(time.Unix(0, 0)), DebugOutputUnavailable{})
		server.debugLogCommand(term.Tuple(term.MustAtom("true"), term.Integer(int64(limit))))
		for _, value := range values {
			server.debugEvent(term.Tuple(term.MustAtom("in"), term.Integer(value)))
		}
		return len(server.debug.events) <= limit && len(server.debug.events) <= len(values)
	}
	switch __gp_m6 := any(result.Of(true, quick.Check(law, &quick.Config{MaxCount: 1000}))).(type) {
	case result.Err[bool, error]:
		cause := __gp_m6.Err
		t.Fatal(cause)
	case result.Ok[bool, error]:
	default:
		panic("goplus: impossible enum value in match")
	}
}

func requestTupleValue(t *testing.T, value term.Term, index int) term.Term {
	t.Helper()
	switch __gp_m7 := any(value).(type) {
	case term.TupleTerm:
		values := __gp_m7.Elements
		return term.Clone(values[index])
	default:
		t.Fatal("request is not tuple")
		return term.InvalidTerm{}
	}
}
